
name: My First Action

on:
  push:
  pull_request:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Get code
        uses: actions/checkout@v4
      - name: Say Hello
        run: python hello.py
